# Required: PostgreSQL connection string (Railway, Neon, or Vercel Postgres).
# For Vercel Edge (/api/ingest) use Neon or Vercel Postgres.
# Example (Neon): postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_URL=postgresql://...

# Optional: Redis URL for future async queue; /api/health reports status.
# REDIS_URL=redis://...

# Optional: Vercel cron secret (secures GET /api/cron/daily). Generate: openssl rand -hex 32
# CRON_SECRET=...

# Optional: AI provider keys for future features.
# OPENAI_API_KEY=...
# ANTHROPIC_API_KEY=...
