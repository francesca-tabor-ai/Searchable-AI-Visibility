export const metadata = {
  title: "Visibility Trends — Searchable",
  description: "Score over time vs top competitors",
};

export default function TrendsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
