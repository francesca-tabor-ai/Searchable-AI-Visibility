export const metadata = {
  title: "Competitive Analysis — Searchable",
  description: "Leaderboard, share of voice, and query overlap vs competitors",
};

export default function CompetitorsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
