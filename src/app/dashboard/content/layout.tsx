export const metadata = {
  title: "Content Performance — Searchable",
  description: "URL leaderboard, citation concentration, and query context",
};

export default function ContentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
